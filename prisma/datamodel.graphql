type User {
  id: ID! @unique
  name: String!
}

type Exercise {
  id: ID! @unique
  name: String!
  bodySection: String
  primaryMover: String
  movementType: String
  trainingPhases: [TrainingPhase!]!
  workoutTypes: [WorkoutType!]!
  equipment: [EquipmentPiece!]!
}

type TrainingPhase {
  name: String!
}

type WorkoutType {
  name: String!
}

type EquipmentPiece {
  name: String!
}